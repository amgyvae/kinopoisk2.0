# 🎬 MovieBase (KinoPoisk Clone)

Учебный проект на Django: веб-платформа для просмотра информации о фильмах, управления отзывами и личным списком «Избранного». Проект доведен до уровня Production-ready с использованием инструментов кэширования и защиты.

---

## 🚀 Основной функционал

* **Каталог фильмов**: Динамический список с фильтрацией по жанрам и странам.
* **Система отзывов**: Возможность оставлять отзывы (только для авторизованных пользователей).
* **Избранное (Favorites)**: AJAX-интерактив для добавления фильмов в личный кабинет.
* **YouTube Player**: Интеграция трейлеров через встраиваемые фреймы (embed).
* **REST API**: Реализованы эндпоинты для получения списка фильмов через Django Rest Framework.

---

## 🛠 Технологический стек

* **Backend**: Django 6.x, Python 3.12.
* **Database**: SQLite (для разработки) / PostgreSQL (опционально).
* **Frontend**: HTML5, CSS3, Bootstrap 5.
* **Performance**: **Redis** (Caching).
* **Security**: **Django-Ratelimit** (Rate Limiting).
* **Environment**: Python-dotenv (хранение SECRET_KEY и паролей).

---

## ⚡️ Продвинутые фичи (Performance & Security)

В рамках проекта были внедрены инструменты для повышения отказоустойчивости и скорости работы:

1.  **Кэширование с Redis**:
    * Часто запрашиваемые страницы (например, список фильмов) кэшируются в Redis.
    * Это снижает нагрузку на базу данных и сокращает время отклика (TTFB).
2.  **Rate Limiting**:
    * Для защиты от спама в отзывах и перебора паролей настроено ограничение частоты запросов с одного IP (5 запросов в минуту на POST-методы).
3.  **Redis Pub/Sub (Experimental)**:
    * Основа для реализации системы уведомлений в реальном времени.

---

## 📦 Как запустить проект

1. **Клонируйте репозиторий:**
    ```bash
    git clone [https://github.com/amgyvae/kinopoisk2.0.git](https://github.com/amgyvae/kinopoisk2.0.git)
    cd KinoPoisk

2. **Создайте и активируйте venv:**
    ```bash
    python -m venv venv
    source venv/bin/activate  # Mac/Linux
    # venv\Scripts\activate   # Windows

3. **Установите зависимости:**
    ```bash
    pip install -r requirements.txt
    Настройте переменные окружения:
    Создайте файл .env в корне и добавьте туда SECRET_KEY.

4. **Запустите Redis:**
    ```bash
    brew services start redis  # Mac

5. **Миграции и запуск:**
   ```bash
    python manage.py migrate
    python manage.py runserver

